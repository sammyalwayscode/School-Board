import React from "react";
import styled from "styled-components";

const Teachers = () => {
  return (
    <Container>
      <Wrapper>Teachers</Wrapper>
    </Container>
  );
};

export default Teachers;

const Container = styled.div``;
const Wrapper = styled.div``;
